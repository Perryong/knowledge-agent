# Knowledge Schema

This folder stores the lightweight knowledge graph compiled from COG notes.

- `concepts/` contains one markdown file per concept, entity, method, setup, tool, company, person, or project.
- `relationships.md` stores graph edges between concepts.
- `logs/` stores schema compilation reports.

Run `/schema-compile` after meaningful new content has been processed.
